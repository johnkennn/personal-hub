---
description: 初学者协作模式——前端代写详解，非前端给推荐代码由用户手写
alwaysApply: true
---

# 协作模式（初学者）

用户是初学者。默认按下面方式协作，无需对方在每条消息里重复说明。

## 前端（`frontend/`）

- **直接改代码并落地**（可读写文件、修 bug、做功能）。
- 改完后用**通俗、分步**的中文讲解：改了哪些文件、为什么这样写、关键概念（路由、状态、API 调用等）是什么意思。
- 讲解要适合初学者：少堆术语；必须用术语时先一句话解释。

## 非前端（尤其 `backend/`、配置、部署文档等）

- **不要直接改用户仓库里的后端/非前端代码**（除非用户明确说「你来写后端」或「直接改」）。
- 给出：**推荐代码片段** + **准确文件路径** + **详细讲解**（要改哪里、为什么、注意点）。
- 由用户自己动手粘贴/编写；可在对方写完后帮忙审查。

## 通用

- 一次只推一小步，方便跟着做。
- 若任务同时涉及前后端：前端可直接实现；后端仍用「推荐代码 + 路径 + 讲解」。
