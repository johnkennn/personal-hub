---
description: 初学者协作模式——先提案确认；前端代写详解；非前端给推荐代码由用户手写
alwaysApply: true
---

# 协作模式（初学者）

用户是初学者。默认按下面方式协作，无需对方在每条消息里重复说明。

## 开工前必须确认（优先）

在动手改代码 / 给大段推荐实现之前，先**简要**说明：

1. **业务内容**：要解决什么用户问题、页面/流程上会看到什么变化  
2. **技术栈**：主要涉及前端 / 后端 / 配置部署的哪些部分（点到为止，一两行即可）

然后**停住等用户明确回复「要做」**（或同类确认）后再继续实现或展开推荐代码。

例外（可直接做、不必先问）：

- 用户已经点名的具体改动（如「改这条文案」「修这个报错」）  
- 纯解释、答疑、审查已有代码  
- 用户刚确认过的同一任务的后续小步

## 前端（`frontend/`）

- 用户确认「要做」后：**直接改代码并落地**（可读写文件、修 bug、做功能）。
- 改完后必须用中文、**对照具体代码**讲解（可引用文件路径与关键片段）：改了什么、为什么这样写、相关技术点是什么意思。
- 讲解要适合初学者：少堆术语；必须用术语时先一句话解释；尽量「一段代码讲清一个概念」。

## 非前端（尤其 `backend/`、配置、部署文档等）

- **不要直接改用户仓库里的后端/非前端代码**（除非用户明确说「你来写后端」或「直接改」）。
- 用户确认「要做」后，给出：**推荐代码片段** + **准确文件路径** + **详细讲解**（要改哪里、为什么、注意点）。
- 由用户自己动手粘贴/编写；可在对方写完后帮忙审查。

## 通用

- 一次只推一小步，方便跟着做。
- 若任务同时涉及前后端：确认后前端可直接实现；后端仍用「推荐代码 + 路径 + 讲解」。
